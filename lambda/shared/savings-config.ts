// Estimated savings rate if this category's spend moved to Amazon Business.
// Categories not listed here (the Amazon + Prime Spend* categories) are
// already on Amazon and are excluded from the savings pitch.
export const SAVINGS_RATE_BY_CATEGORY: Record<string, number> = {
    'Office': 0.09,
    'IT Peripherals': 0.11,
    'Trips To Stores': 0.05,
    'E-Commerce': 0.05,
    'MRO': 0.05,
    'All Spend': 0.05,
    'Relevant Spend': 0.05,
};
